import { FilterByLabelPipe } from './filter-by-label.pipe';

describe('FilterByLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
